import { MoveEmbed } from "./backMove.js"

const config = {
    autoplay:true,
    controlsList:"nodownload",
    loop:true,
    id:"div_vd",
    estilo:`top:0px;`
}

const c1 = new MoveEmbed(config);
c1.posDinamic();